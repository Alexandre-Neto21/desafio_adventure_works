version: 2

models:
  - name: stg_adventure_works__address
    description: Address raw informations
    columns:
      - name: address_id
        description: Primary key of the table
        tests:
          - unique
          - not_null
