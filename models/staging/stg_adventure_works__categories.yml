version: 2

models:
  - name: stg_adventure_works__categories
    description: Categories raw informations
    columns:
      - name: category_id
        description: Primary key of the table
        tests:
          - unique
          - not_null
