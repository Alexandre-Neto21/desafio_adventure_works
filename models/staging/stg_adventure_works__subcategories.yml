version: 2

models:
  - name: stg_adventure_works__subcategories
    description: Subcategories raw informations
    columns:
      - name: subcategory_id
        description: Primary key of the table
        tests:
          - unique
          - not_null
          