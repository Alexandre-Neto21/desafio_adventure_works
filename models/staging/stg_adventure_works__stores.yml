version: 2

models:
  - name: stg_adventure_works__stores
    description: Stores raw informations
    columns:
      - name: business_entity_id
        description: Primary key of the table
        tests:
          - unique
          - not_null