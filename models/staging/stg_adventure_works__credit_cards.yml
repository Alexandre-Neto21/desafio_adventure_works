version: 2

models:
  - name: stg_adventure_works__credit_cards
    description: Credit card raw informations
    columns:
      - name: credit_card_id
        description: Primary key of the table
        tests:
          - unique
          - not_null